'use client'

import { useCallback, useEffect, useMemo, useState } from 'react'
import Link from 'next/link'
import { useRouter, useSearchParams } from 'next/navigation'
import { UZBEKISTAN_LOCATIONS } from '@/data/uzbekistan-locations'

type Lang = 'uz' | 'ru'
type Tab = 'sale' | 'rent' | 'daily' | 'all'
type Listing = {
  id: string; title: string; title_ru?: string | null; listing_type: string; property_type: string | null
  price: number; currency: string; area_m2: number | null; rooms: number | null; district: string | null; city: string
  seller_type: string; seller_name: string | null; is_mortgage_available: boolean; is_verified: boolean
  is_trusted_seller: boolean; is_featured: boolean; published_at: string | null; taxonomy_code?: string | null
  primary_image?: { image_url: string; sort_order: number | null } | null
}
type SearchState = { tab: Tab; region: string; district: string; type: string; min: string; max: string; currency: string; rooms: string; verified: boolean; owner: boolean; mortgage: boolean; taxonomy: string; sort: string; q: string }

const money = (v: number, c: string) => `${new Intl.NumberFormat('ru-RU').format(Number(v))} ${c === 'USD' ? '$' : 'so‘m'}`
const titleOf = (x: Listing, l: Lang) => l === 'ru' ? (x.title_ru || x.title) : x.title
const typeLabel = (v: string | null | undefined, l: Lang) => ({ apartment: l === 'ru' ? 'Квартира' : 'Kvartira', house: l === 'ru' ? 'Частный дом' : 'Xususiy uy', land: l === 'ru' ? 'Земля' : 'Yer', commercial: l === 'ru' ? 'Коммерция' : 'Tijorat', new_building: l === 'ru' ? 'Новостройка' : 'Yangi bino' }[v || ''] || v || '')

function readState(params: URLSearchParams): SearchState {
  const tab = params.get('tab')
  return { tab: tab === 'rent' || tab === 'daily' || tab === 'all' ? tab : 'sale', region: params.get('region') || '', district: params.get('district') || '', type: params.get('type') || '', min: params.get('min') || '', max: params.get('max') || '', currency: params.get('currency') || '', rooms: params.get('rooms') || '', verified: params.get('verified') === 'true', owner: params.get('owner') === 'true', mortgage: params.get('mortgage') === 'true', taxonomy: params.get('taxonomy') || '', sort: params.get('sort') || 'newest', q: params.get('q') || '' }
}
function toQuery(s: SearchState, page: number) {
  const q = new URLSearchParams(); q.set('tab', s.tab); q.set('page', String(page)); q.set('limit', '24'); q.set('sort', s.sort)
  const values: [string, string][] = [['region', s.region], ['district', s.district], ['property_type', s.type], ['min', s.min], ['max', s.max], ['currency', s.currency], ['rooms', s.rooms], ['taxonomy', s.taxonomy], ['q', s.q]]
  values.forEach(([k, v]) => { if (v) q.set(k, v) })
  if (s.verified) q.set('verified', 'true'); if (s.owner) q.set('owner', 'true'); if (s.mortgage) q.set('mortgage', 'true')
  return q
}

export default function ListingsPage() {
  const router = useRouter(); const searchParams = useSearchParams()
  const [lang, setLang] = useState<Lang>('uz'); const [state, setState] = useState<SearchState>(() => readState(new URLSearchParams(searchParams.toString())))
  const [items, setItems] = useState<Listing[]>([]); const [total, setTotal] = useState<number | null>(null); const [hasNext, setHasNext] = useState(false); const [page, setPage] = useState(1)
  const [loading, setLoading] = useState(true); const [error, setError] = useState(''); const [saveOpen, setSaveOpen] = useState(false); const [saveName, setSaveName] = useState(''); const [saveBusy, setSaveBusy] = useState(false); const [saveMessage, setSaveMessage] = useState('')

  useEffect(() => { const saved = localStorage.getItem('prohouse-lang'); if (saved === 'ru') setLang('ru') }, [])
  useEffect(() => { const next = readState(new URLSearchParams(searchParams.toString())); setState(next); setPage(Number(searchParams.get('page') || '1') || 1) }, [searchParams])

  const queryString = useMemo(() => toQuery(state, page).toString(), [state, page])
  const load = useCallback(async () => {
    setLoading(true); setError('')
    try { const response = await fetch(`/api/listings/search?${queryString}`, { cache: 'no-store' }); const result = await response.json().catch(() => ({})); if (!response.ok) throw new Error(result.error || 'E’lonlarni yuklab bo‘lmadi.'); setItems((result.data || []) as Listing[]); setTotal(result.pagination?.total ?? null); setHasNext(Boolean(result.pagination?.has_next)) }
    catch (e) { setItems([]); setTotal(null); setHasNext(false); setError(e instanceof Error ? e.message : 'Xatolik yuz berdi.') }
    finally { setLoading(false) }
  }, [queryString])
  useEffect(() => { void load() }, [load])

  const districts = useMemo(() => state.region ? UZBEKISTAN_LOCATIONS.find(x => x.name === state.region)?.districts ?? [] : [], [state.region])
  const text = lang === 'ru' ? { title: 'Объявления', sub: 'Найдите подходящую недвижимость и услуги', filters: 'Фильтры', location: 'Расположение', district: 'Район / город', type: 'Тип недвижимости', currency: 'Валюта', rooms: 'Комнаты', from: 'от', to: 'до', owner: 'От владельца', mortgage: 'Ипотека доступна', verified: 'Проверено', apply: 'Применить', clear: 'Сбросить', all: 'Все типы', empty: 'По вашему запросу объявлений нет.', newest: 'Новые', low: 'Дешевле', high: 'Дороже', save: 'Сохранить поиск', saveTitle: 'Название поиска', savePlaceholder: 'Например: 3-комнатная в Ташкенте', saveConfirm: 'Сохранить', cancel: 'Отмена', prev: 'Назад', next: 'Далее', loading: 'Загрузка...', saved: 'Поиск сохранён' } : { title: 'E’lonlar', sub: 'Sizga mos ko‘chmas mulk va xizmatlarni toping', filters: 'Filtrlar', location: 'Joylashuv', district: 'Tuman / shahar', type: 'Ko‘chmas mulk turi', currency: 'Valyuta', rooms: 'Xonalar', from: 'dan', to: 'gacha', owner: 'Egadan', mortgage: 'Ipotekaga mumkin', verified: 'Tasdiqlangan', apply: 'Qo‘llash', clear: 'Tozalash', all: 'Barcha turlar', empty: 'Tanlangan shartlar bo‘yicha e’lon topilmadi.', newest: 'Eng yangi', low: 'Arzonidan', high: 'Qimmatidan', save: 'Qidiruvni saqlash', saveTitle: 'Qidiruv nomi', savePlaceholder: 'Masalan: Toshkentda 3 xonali', saveConfirm: 'Saqlash', cancel: 'Bekor qilish', prev: 'Orqaga', next: 'Keyingi', loading: 'Yuklanmoqda...', saved: 'Qidiruv saqlandi' }
  const tabs: [Tab, string][] = [['sale', lang === 'ru' ? 'Продажа' : 'Sotuv'], ['rent', lang === 'ru' ? 'Аренда' : 'Ijara'], ['daily', lang === 'ru' ? 'Посуточно' : 'Kunlik'], ['all', lang === 'ru' ? 'Все' : 'Barchasi']]
  const update = (patch: Partial<SearchState>) => setState(s => ({ ...s, ...patch }))
  const apply = (next = state, nextPage = 1) => router.push(`/listings?${toQuery(next, nextPage).toString()}`)
  const clear = () => { const next: SearchState = { tab: 'sale', region: '', district: '', type: '', min: '', max: '', currency: '', rooms: '', verified: false, owner: false, mortgage: false, taxonomy: '', sort: 'newest', q: '' }; setState(next); apply(next) }
  const saveSearch = async () => { setSaveBusy(true); setSaveMessage(''); try { const response = await fetch('/api/searches', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ name: saveName.trim() || `${lang === 'ru' ? 'Поиск' : 'Qidiruv'} ${new Date().toLocaleDateString()}`, query: state, notifyPush: true }) }); const result = await response.json().catch(() => ({})); if (response.status === 401) { router.push('/register?redirect=/listings'); return } if (!response.ok) throw new Error(result.error || 'Qidiruvni saqlab bo‘lmadi.'); setSaveMessage(text.saved); setSaveName(''); setTimeout(() => setSaveOpen(false), 700) } catch (e) { setSaveMessage(e instanceof Error ? e.message : 'Xatolik yuz berdi.') } finally { setSaveBusy(false) } }

  return <main className="min-h-screen bg-[#f6f7f8] text-slate-900"><header className="border-b bg-white"><div className="mx-auto flex h-16 max-w-[1440px] items-center justify-between px-4"><Link href="/" className="text-xl font-black">Pro<span className="text-emerald-500">house</span></Link><div className="flex items-center gap-2"><Link href="/account/saved-searches" className="hidden rounded-xl border px-3 py-2 text-xs font-bold sm:block">⌕ {lang === 'ru' ? 'Сохранённые поиски' : 'Saqlangan qidiruvlar'}</Link><Link href="/account/favorites" className="hidden rounded-xl border px-3 py-2 text-xs font-bold sm:block">♡ {lang === 'ru' ? 'Избранное' : 'Saqlanganlar'}</Link><button onClick={() => { const n = lang === 'uz' ? 'ru' : 'uz'; setLang(n); localStorage.setItem('prohouse-lang', n) }} className="rounded-xl border px-3 py-2 text-xs font-bold">{lang === 'uz' ? 'O‘z / Ru' : 'Ru / O‘z'}</button></div></div></header><section className="border-b bg-white"><div className="mx-auto max-w-[1440px] px-4 py-6"><Link href="/" className="text-xs text-slate-500">{lang === 'ru' ? 'Главная' : 'Bosh sahifa'}</Link><h1 className="mt-3 text-3xl font-black">{text.title}</h1><p className="mt-1 text-sm text-slate-500">{text.sub}</p>{state.taxonomy && <div className="mt-3 inline-flex rounded-full bg-emerald-50 px-3 py-1 text-xs font-bold text-emerald-700">{state.taxonomy}</div>}</div></section><div className="mx-auto max-w-[1440px] px-4 py-5"><div className="mb-5 flex gap-2 overflow-x-auto rounded-2xl border bg-white p-1.5 shadow-sm">{tabs.map(([v, l]) => <button key={v} onClick={() => apply({ ...state, tab: v })} className={`rounded-xl px-5 py-2.5 text-sm font-bold ${state.tab === v ? 'bg-emerald-500 text-white' : 'text-slate-600'}`}>{l}</button>)}</div><div className="grid gap-5 lg:grid-cols-[280px_minmax(0,1fr)]"><aside className="h-fit rounded-2xl border bg-white p-5 shadow-sm"><div className="mb-5 flex justify-between"><b>{text.filters}</b><button onClick={clear} className="text-xs font-bold text-emerald-600">{text.clear}</button></div><label className="block text-sm font-semibold">{text.location}<select value={state.region} onChange={e => update({ region: e.target.value, district: '' })} className="mt-2 h-11 w-full rounded-xl border px-3"><option value="">{lang === 'ru' ? 'Весь Узбекистан' : 'Butun O‘zbekiston'}</option>{UZBEKISTAN_LOCATIONS.map(x => <option key={x.name} value={x.name}>{x.name}</option>)}</select></label><label className="mt-4 block text-sm font-semibold">{text.district}<select disabled={!state.region} value={state.district} onChange={e => update({ district: e.target.value })} className="mt-2 h-11 w-full rounded-xl border px-3 disabled:bg-slate-100"><option value="">{lang === 'ru' ? 'Все районы и города' : 'Barcha tuman va shaharlar'}</option>{districts.map(x => <option key={x} value={x}>{x}</option>)}</select></label><label className="mt-4 block text-sm font-semibold">{text.type}<select value={state.type} onChange={e => update({ type: e.target.value })} className="mt-2 h-11 w-full rounded-xl border px-3"><option value="">{text.all}</option>{['apartment', 'house', 'land', 'commercial', 'new_building'].map(x => <option key={x} value={x}>{typeLabel(x, lang)}</option>)}</select></label><div className="mt-4"><p className="text-sm font-semibold">{text.currency}</p><div className="mt-2 grid grid-cols-3 gap-2">{[['', 'All'], ['UZS', 'so‘m'], ['USD', '$']].map(([v, l]) => <button key={v || 'all'} onClick={() => update({ currency: v })} className={`rounded-lg border px-2 py-2 text-xs font-bold ${state.currency === v ? 'border-emerald-500 bg-emerald-50' : ''}`}>{l}</button>)}</div></div><div className="mt-4 grid grid-cols-2 gap-2"><input value={state.min} onChange={e => update({ min: e.target.value })} inputMode="numeric" placeholder={text.from} className="h-11 rounded-xl border px-3"/><input value={state.max} onChange={e => update({ max: e.target.value })} inputMode="numeric" placeholder={text.to} className="h-11 rounded-xl border px-3"/></div><div className="mt-4"><p className="text-sm font-semibold">{text.rooms}</p><div className="mt-2 grid grid-cols-4 gap-2">{['1', '2', '3', '4'].map(v => <button key={v} onClick={() => update({ rooms: state.rooms === v ? '' : v })} className={`rounded-lg border py-2 text-sm font-bold ${state.rooms === v ? 'border-emerald-500 bg-emerald-50' : ''}`}>{v === '4' ? '4+' : v}</button>)}</div></div><div className="mt-5 space-y-3 border-t pt-4 text-sm"><label className="flex gap-3"><input type="checkbox" checked={state.owner} onChange={e => update({ owner: e.target.checked })}/>{text.owner}</label><label className="flex gap-3"><input type="checkbox" checked={state.mortgage} onChange={e => update({ mortgage: e.target.checked })}/>{text.mortgage}</label><label className="flex gap-3"><input type="checkbox" checked={state.verified} onChange={e => update({ verified: e.target.checked })}/>{text.verified}</label></div><button onClick={() => apply(state)} className="mt-4 w-full rounded-xl bg-emerald-600 py-3 text-sm font-black text-white">{text.apply}</button><button onClick={() => { setSaveOpen(true); setSaveMessage('') }} className="mt-2 w-full rounded-xl border border-emerald-200 bg-emerald-50 py-3 text-sm font-black text-emerald-700">⌕ {text.save}</button></aside><section><div className="mb-4 flex flex-wrap items-center justify-between gap-3"><b>{loading ? '—' : `${total ?? items.length} ${lang === 'ru' ? 'объявлений' : 'ta e’lon'}`}</b><div className="flex items-center gap-2"><select value={state.sort} onChange={e => apply({ ...state, sort: e.target.value })} className="rounded-xl border bg-white px-3 py-2 text-sm"><option value="newest">{text.newest}</option><option value="priceLow">{text.low}</option><option value="priceHigh">{text.high}</option></select><button onClick={() => { setSaveOpen(true); setSaveMessage('') }} className="rounded-xl border border-emerald-200 bg-white px-3 py-2 text-sm font-extrabold text-emerald-700">⌕ {text.save}</button></div></div>{loading && <p className="mb-3 text-xs text-slate-400">{text.loading}</p>}{error && <div className="mb-4 rounded-2xl border border-red-200 bg-red-50 p-4 text-sm text-red-700">{error}</div>}{!loading && !error && !items.length && <div className="rounded-2xl border bg-white p-10 text-center text-slate-500">{text.empty}</div>}<div className="grid gap-4 sm:grid-cols-2">{items.map(x => { const image = x.primary_image?.image_url; const period = x.listing_type === 'rent' ? ' / oy' : x.listing_type === 'daily' ? ' / kun' : ''; const isService = x.listing_type === 'service' || x.listing_type === 'realtor'; return <Link key={x.id} href={`/listings/${x.id}`} className="group overflow-hidden rounded-2xl border bg-white shadow-sm transition hover:-translate-y-0.5 hover:shadow-lg"><div className="relative aspect-[4/3] overflow-hidden bg-slate-100">{image ? <img src={image} alt={titleOf(x, lang)} loading="lazy" className="h-full w-full object-cover transition duration-500 group-hover:scale-105"/> : <div className="flex h-full items-center justify-center text-slate-400">{lang === 'ru' ? 'Нет фото' : 'Rasm yo‘q'}</div>}{x.is_featured && <span className="absolute left-3 top-3 rounded-lg bg-amber-400 px-2 py-1 text-xs font-black">TOP</span>}{x.is_verified && <span className="absolute right-3 top-3 rounded-lg bg-white px-2 py-1 text-xs font-bold text-emerald-700">✓ {lang === 'ru' ? 'Проверено' : 'Tasdiqlangan'}</span>}{x.is_trusted_seller && <span className="absolute bottom-3 left-3 rounded-full bg-emerald-50 px-2.5 py-1.5 text-[11px] font-extrabold text-emerald-700">✓ {lang === 'ru' ? 'Надёжный профиль' : 'Ishonchli profil'}</span>}{x.seller_type === 'owner' && <span className="absolute bottom-3 right-3 rounded-full bg-white px-2.5 py-1.5 text-[11px] font-extrabold text-emerald-700">{lang === 'ru' ? 'От владельца' : 'Egadan'}</span>}</div><div className="p-4"><h2 className="text-lg font-black">{money(x.price, x.currency)}{period}</h2><p className="mt-1 line-clamp-2 text-sm font-bold">{titleOf(x, lang)}</p><p className="mt-2 text-xs text-slate-500">⌖ {x.city}{x.district ? `, ${x.district}` : ''}</p><div className="mt-3 flex flex-wrap gap-1.5 text-xs"><span className="rounded-md bg-slate-100 px-2 py-1">{isService ? (x.listing_type === 'realtor' ? (lang === 'ru' ? 'Риелтор' : 'Rieltor') : (lang === 'ru' ? 'Услуга' : 'Xizmat')) : typeLabel(x.property_type, lang)}</span>{x.area_m2 != null && !isService && <span className="rounded-md bg-slate-100 px-2 py-1">{x.area_m2} m²</span>}{x.rooms != null && !isService && <span className="rounded-md bg-slate-100 px-2 py-1">{x.rooms} {lang === 'ru' ? 'комн.' : 'xona'}</span>}</div><div className="mt-4 border-t pt-3 text-xs font-semibold text-slate-600">{x.seller_name || (lang === 'ru' ? 'Продавец' : 'Sotuvchi')}</div></div></Link> })}</div><div className="mt-6 flex items-center justify-center gap-3"><button disabled={page <= 1 || loading} onClick={() => apply(state, page - 1)} className="rounded-xl border bg-white px-4 py-2 text-sm font-bold disabled:opacity-40">{text.prev}</button><span className="rounded-xl bg-white px-4 py-2 text-sm font-bold">{page}</span><button disabled={!hasNext || loading} onClick={() => apply(state, page + 1)} className="rounded-xl border bg-white px-4 py-2 text-sm font-bold disabled:opacity-40">{text.next}</button></div></section></div></div>{saveOpen && <div className="fixed inset-0 z-50 flex items-center justify-center bg-slate-950/45 px-4" role="dialog" aria-modal="true"><div className="w-full max-w-md rounded-3xl bg-white p-6 shadow-2xl"><div className="flex items-start justify-between gap-4"><div><h2 className="text-xl font-black">{text.save}</h2><p className="mt-1 text-sm text-slate-500">{text.saveTitle}</p></div><button onClick={() => setSaveOpen(false)} className="rounded-full bg-slate-100 px-3 py-1 text-lg">×</button></div><input autoFocus value={saveName} onChange={e => setSaveName(e.target.value)} onKeyDown={e => { if (e.key === 'Enter') void saveSearch() }} placeholder={text.savePlaceholder} className="mt-5 h-12 w-full rounded-xl border border-slate-200 px-4 outline-none focus:border-emerald-500"/>{saveMessage && <p className="mt-3 rounded-xl bg-emerald-50 px-3 py-2 text-sm font-semibold text-emerald-700">{saveMessage}</p>}<div className="mt-5 flex gap-3"><button onClick={() => setSaveOpen(false)} className="flex-1 rounded-xl border border-slate-200 py-3 font-bold text-slate-700">{text.cancel}</button><button disabled={saveBusy} onClick={() => void saveSearch()} className="flex-1 rounded-xl bg-emerald-600 py-3 font-black text-white disabled:opacity-50">{saveBusy ? '...' : text.saveConfirm}</button></div></div></div>}</main>
}
