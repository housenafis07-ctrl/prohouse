'use client'
import Link from 'next/link'
import { useEffect,useState } from 'react'
import { createClient } from '@/utils/supabase/client'
type Realtor={id:string;display_name:string;bio:string|null;avatar_url:string|null;experience_years:number;verification_status:string;rating:number;reviews_count:number;agency_id:string|null}
export default function RealtorsPage(){const[items,setItems]=useState<Realtor[]>([]);useEffect(()=>{createClient().from('realtor_profiles').select('*').in('verification_status',['pending','verified']).order('rating',{ascending:false}).then(({data})=>setItems(data||[]))},[]);return <main className="min-h-screen bg-slate-50"><header className="border-b bg-white"><div className="mx-auto flex max-w-7xl items-center justify-between px-4 py-5"><Link href="/" className="text-2xl font-black">Pro<span className="text-emerald-500">house</span></Link><Link href="/account" className="rounded-xl bg-emerald-600 px-4 py-2 text-sm font-bold text-white">Kabinet</Link></div></header><section className="mx-auto max-w-7xl px-4 py-10"><p className="text-xs font-black tracking-[.18em] text-emerald-600">PROFESSIONALS</p><h1 className="mt-2 text-4xl font-black">Rieltorlar</h1><p className="mt-3 text-slate-500">Tasdiqlangan ko‘chmas mulk mutaxassislarini toping.</p><div className="mt-8 grid gap-5 md:grid-cols-2 lg:grid-cols-3">{items.map(r=><Link key={r.id} href={`/realtors/${r.id}`} className="rounded-3xl border bg-white p-5 shadow-sm transition hover:-translate-y-0.5"><div className="flex items-center gap-4"><div className="flex h-16 w-16 items-center justify-center overflow-hidden rounded-2xl bg-slate-100 font-black">{r.avatar_url?<img src={r.avatar_url} alt="" className="h-full w-full object-cover"/>:'R'}</div><div><h2 className="font-black">{r.display_name}</h2><p className="text-xs text-emerald-600">{r.verification_status==='verified'?'✓ Tasdiqlangan':'Professional rieltor'}</p></div></div><p className="mt-4 line-clamp-2 text-sm text-slate-500">{r.bio||'Ko‘chmas mulk bo‘yicha professional xizmat.'}</p><div className="mt-5 flex justify-between text-sm font-bold"><span>★ {Number(r.rating).toFixed(1)} ({r.reviews_count})</span><span>{r.experience_years} yil tajriba</span></div></Link>)}</div>{!items.length&&<div className="mt-8 rounded-3xl bg-white p-12 text-center text-slate-500">Hozircha tasdiqlangan professional rieltorlar yo‘q.</div>}</section></main>}
