'use client'
import { useEffect } from 'react'
export default function DailySearchNavigationFix(){useEffect(()=>{const onClick=(e:MouseEvent)=>{if(location.pathname!=='/')return;const t=e.target as HTMLElement|null;const b=t?.closest('button') as HTMLButtonElement|null;if(!b)return;const l=b.textContent?.trim()||'';if(l!=='Qidirish'&&l!=='Найти')return;const daily=Array.from(document.querySelectorAll('button')).find(x=>{const s=x.textContent?.trim();return s==='Kunlik'||s==='Посуточно'}) as HTMLButtonElement|undefined;if(!daily||!daily.className.includes('bg-emerald-500'))return;e.preventDefault();e.stopPropagation();const s=Array.from(document.querySelectorAll('select')) as HTMLSelectElement[];const p=new URLSearchParams();if(s[0]?.value)p.set('region',s[0].value);if(s[1]?.value)p.set('district',s[1].value);location.assign(`/daily${p.toString()?`?${p.toString()}`:''}`)};document.addEventListener('click',onClick,true);return()=>document.removeEventListener('click',onClick,true)},[]);return null}
